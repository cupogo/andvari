version: "2"

run:
  timeout: 10m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

  settings:
    govet:
      disable:
        - inline
    staticcheck:
      checks:
        - all
        - -ST1000
        - -ST1003